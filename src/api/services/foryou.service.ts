export const getForYouFeed = async (
    userId: string,
    page: number = 1,
    limit: number = 5,
    location?: {
        lng: number;
        lat: number;
    }
) => {
    // get the user
    // get user seens
    // get userActivities with media
    // sorted by date
    // sorted by location area
    // sorted by user following
    // 
}